import { UniqueEntriesPipe } from './unique-entries.pipe';

describe('UniqueEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
