import { ThemeByIndexPipe } from './theme-by-index.pipe';

describe('ThemeByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
