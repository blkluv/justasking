import { AlphaNumericsOnlyDirective } from './alpha-numerics-only.directive';

describe('AlphaNumericsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaNumericsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
